export async function createSmartContract(address1: string, address2: string, address3: string) {
  // Triển khai contract 3 bên trên blockchain
  // ...
}

export async function signAndCreateNewContract(product: any, shipperId: string, nextReceiver: string) {
  // Ký hợp đồng cũ, tạo hợp đồng mới
  // ...
} 